namespace Grapht.Exception {
    /// <summary>
    /// Thrown when a bad step (a step that would result in an infinite sequence) is given for a range
    /// </summary>
    public class BadRangeException : System.Exception {
    }
}
